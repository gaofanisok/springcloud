spring:
  application:
    name: client-test

#服务端口
server:
  port: 8083

# 注册中心地址
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
  instance:
    prefer-ip-address: true


feign:
  hystrix:
    enabled: true